spring:
  profiles:
    # prod, dev
    active: dev
  application:
    name: fox-demo-message

# 暴露端点
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
